/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['avatars.githubusercontent.com', '192.168.1.7'], // quais domínios vao ser permitidos
  },
}

module.exports = nextConfig
